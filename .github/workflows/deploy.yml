name: Build and Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Replace secrets in JS
        run: |
          sed -i "s|__AUTH_EMAIL__|${{ secrets.AUTH_EMAIL }}|g" ../templatemo-3d-coverflow-scripts.js
          sed -i "s|__AUTH_PASSWORD__|${{ secrets.AUTH_PASSWORD }}|g" ../templatemo-3d-coverflow-scripts.js
          sed -i "s|__URL_ENDPOINT__|${{ secrets.URL_ENDPOINT }}|g" ../templatemo-3d-coverflow-scripts.js

      - name: Build and deploy  
        run: |
          # misal kalau kamu pakai npm build atau rsync ke server
          echo "🚀 Build complete"
